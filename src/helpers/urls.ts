export function dashboardUrl(accountId: number) {
  return `https://quicknode.com/dashboard/${accountId}`
}

export function accessUrl(accountId: number) {
  return `https://quicknode.com/access/${accountId}`
}
